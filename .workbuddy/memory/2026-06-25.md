## 2026-06-25

### 沈家本研究院官网 — Phase 1-3 完整开发

**Phase 1-2 前端开发**

**完成工作：**
- 项目结构初始化，需求文档归档（docs/REQUIREMENTS.md）
- 全站 CSS 变量体系（main.css）：严格遵循 UI 规范色值、字体、间距、圆角、阴影
- 响应式断点（responsive.css）：平板 768-1024px、移动端 <768px、小屏 <480px
- 公共 JS 工具库（common.js）：API 封装、防抖、导航交互、分页组件、搜索筛选联动、弹窗、表单提交、加载/空/异常兜底
- Phase 1 前台页面（9 个）：
  - index.html — 首页（Hero + 五大模块统计卡片 + 最新动态 + 研究院简介 + 合作机构 + 页脚）
  - about.html — 研究院概况（机构介绍、组织架构、发展历程时间轴、研究员队伍）
  - research.html — 文化研究中心（史料文库、著作、课题、研学）
  - publications.html — 成果出版物（分类/年份/关键词筛选 + 分页）
  - cooperation.html — 合作交流（合作伙伴、学术会议、国际交流）
  - mediation.html — 商事调解中心（介绍、流程、团队、咨询表单）
  - events.html — 活动专栏（筛选 + 分页 + 详情弹窗）
  - event-register.html — 活动报名表单
  - contact.html — 联系我们（联系方式 + 留言表单 + 地图）
- Phase 2 动态数据页面（5 个，全部含 ECharts）：
  - discipline.html — 律师行政处罚查询（搜索联想 + 省份/类型/时间筛选 + 分页表格 + 月度趋势/省份分布/违规占比 3 图表 + 详情弹窗）
  - legislation.html — 立法进度追踪（筛选 + 卡片列表 + 立法进度时间轴弹窗 + 阶段统计图表）
  - cases.html — 热点案件评析（案由/进度/法院筛选 + 列表 + 案由饼图 + 季度趋势线图 + 详情弹窗）
  - policy.html — 司法政策汇总（分类/年份/发布机关筛选 + 列表 + 原文跳转）
  - recruitment.html — 法律招聘聚合（城市/类型/薪资筛选 + 列表 + 30天趋势/城市分布/岗位占比 3 图表 + 详情弹窗）
- Phase 3 后台管理（admin/index.html）：数据仪表盘 + 五大数据台账 + 留言报名管理 + 采集配置 + 账号管理 + Excel 导出

**全部页面遵循：**
- 所有动态数据 100% API 驱动（调用 /api/v1/ 接口），无硬编码假数据
- 统一加载/空数据/异常兜底（"数据加载中…" / "暂无匹配数据" / "数据加载异常，请稍后重试"）
- 全站交互监听：输入防抖搜索、筛选联动、分页无刷新、hover 反馈、弹窗详情、图表联动
- 严格色值规范（#203B64 法治深蓝、#C89959 古铜点缀等）
- 统一合规页脚（ICP 备案、数据来源声明、免责声明、反馈入口）
- PC + 移动端 100% 响应式

**已修正问题：**
- Hero Banner 生卒年从错误的 "1902-1913" 修正为 "1840-1913"（1902 是修律时期，非生年）

### Phase 3 完整后端开发 (Node.js 纯内置模块)

**后端架构（零外部依赖，立即可运行）：**
- `src/backend-js/server.js` — 完整 HTTP API 服务器（纯 Node.js http 模块）
- `src/backend-js/db.js` — JSON 文件数据库引擎，支持 CRUD、分页、筛选、排序、聚合统计（QueryBuilder 模式）
- `src/backend-js/seed.js` — 种子数据生成器（80 条律师处罚 + 12 条立法 + 10 条案件 + 10 条政策 + 50 条招聘）
- `src/backend-js/api/` — API 路由模块目录

**API 路由（43+ 端点，全部验证通过）：**
- 首页：/home/stats, /home/feed
- 律师处罚：/discipline/list, /detail/:id, /stats, /search, /provinces
- 立法追踪：/legislation/list, /detail/:id, /timeline/:id, /search, /stages
- 案件评析：/cases/list, /detail/:id, /stats
- 司法政策：/policy/list, /detail/:id
- 法律招聘：/recruitment/list, /detail/:id, /search, /stats, /cities
- 后台管理：/admin/login, /dashboard, /messages, /registrations, /collection-logs, /content
- 公开接口：/messages (POST), /event-register (POST), /content, /compliance, /health

**Python 后端备选方案（已完成构建，待网络恢复可切换）：**
- `src/backend/` — Flask + SQLAlchemy 完整实现（因 PyPI SSL 代理问题未安装依赖）
- 包含完整的 Flask 应用、ORM 模型、API 蓝图、数据采集器、调度器

**部署配置（完整）：**
- `deploy/nginx.conf` — Nginx 反向代理 + 静态文件服务 + API 代理
- `deploy/shenjiaben.service` — systemd 服务配置
- `deploy/deploy.sh` — 一键部署脚本（Ubuntu/CentOS）
- `deploy/backup.sh` — 数据库自动备份（crontab）
- `start.bat` — Windows 一键启动

### 前后端一体化 + CloudStudio 部署

- 更新 server.js 加入静态文件托管（fs/path 内置模块），同一端口服务前端页面 + API 接口
- 修复 serveStatic 403 路径导致的双重响应头 bug（`Error: Cannot write headers after they are sent`）
- 创建 dist/ 部署目录
- **CloudStudio 部署成功**: https://23b82b587ace40da9611cfa25dbcf862.app.codebuddy.work
- 本地服务运行中: http://localhost:5000（同时托管 API + 静态文件）

### API 数据渲染 + 字段映射修复

**问题**：动态页面调用 API 后数据无法显示，原因包括：
1. 前后端字段名不一致（server 用 `lawFirm`，前端用 `firm` 等）
2. API 响应包裹层不一致（server `{code, data}` vs 前端期望直接 `data`）
3. 部分 API 路由缺失（首页 /home/latest、内容页 /about/* 等）

**修复内容**：
- `common.js`：API get() 自动解包 `json.data`
- `index.html`：stats 字段名修正为 `lawyer_discipline` 等；latest 适配 `data.feed`
- `server.js`：新增 20+ 条缺失 API 路由（about/research/cooperation/contact/mediation/events/publications/legislation/stats）
- `server.js`：所有列表/详情 handler 添加字段映射（discipline/legislation/cases/recruitment），统一前端期望字段名

### Render.com 免费部署配置
- 创建 `.gitignore`（排除 node_modules/venv/data/dist）
- 创建 `render.yaml`（Blueprint 一键部署：新加坡节点、免费套餐、自动休眠唤醒）
- 创建 `DEPLOY_RENDER.md`（完整部署指南 + UptimeRobot 防休眠方案）
- 初始化 Git 仓库，56 文件首次提交成功
- GitHub CLI 未安装，需用户在 GitHub 网站手动创建仓库后执行 `git remote add` + `git push`
- `db.js`：execute() 返回扁平格式 `{items, total, page, pageSize, totalPages}`；新增 events/publications/content_pages 表
- `seed.js`：新增 events（7条）、publications（10条）种子数据
- 动态路由添加无 `/detail/` 前缀的别名路径

### 免费部署方案（Render.com）

- 创建 `package.json`（Node.js 启动配置）
- 编写完整免费部署指南 `docs/FREE_DEPLOYMENT.md`
- Render.com 方案：零成本全栈托管（750h/月，自动 HTTPS）
- 备选方案：GitHub Pages（纯前端）、Vercel + Render 组合
- CloudStudio 预览更新至最新版：https://23b82b587ace40da9611cfa25dbcf862.app.codebuddy.work
